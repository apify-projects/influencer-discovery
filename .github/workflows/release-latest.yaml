name: Release latest

on:
    push:
        branches: [ master ]

jobs:
    buildLatest:
        uses: apify-projects/store-github-actions-source/.github/workflows/push-build-latest.yaml@master
        secrets: inherit
